mkdir mumbaigit
ls (to show the dir or file)
cd mumbaigit
git init
touch myfile (put some data)
git status
git add .
git commit -m "1st commit from mumbai"
git status
git show <commit id)
git remote add origin <centralgit.url>
git push -u  origin master (enter user name and password)
Singapur git
sudo su
yum update -y
yum install git -y
which git (To see the path of git)
git --version (To see the version of git)
git config --global user.name "tarun"
git config --global user.eamil "tarunz67@gmail.com"
git config --list (To see the user name and E-mail)
mkdir singapurgit
ls (to show the dir or file)
cd singapurgit
git init
git status
git add .
git remote add origin <centralgit.url>
git pull origin master
