mkdir mumbaigit
ls (to show the dir or file)
cd mumbaigit
git init
touch myfile (put some data)
git status
git add .
git commit -m "1st commit from mumbai"
git status
git show <commit id)
git remote add origin <centralgit.url>
git push -u  origin master (enter user name and password)
